--- /tmp/bauerbill/build/community/dwm/PKGBUILD.bak	2010-09-24 20:06:52.974588614 -0400
+++ /tmp/bauerbill/build/community/dwm/PKGBUILD	2010-09-24 20:08:24.585771086 -0400
@@ -12,6 +12,7 @@
 license=('MIT')
 options=(zipman)
 depends=('libx11' 'libxinerama')
+patchdir="$HOME/src/community/dwm"
 install=dwm.install
 source=(http://dl.suckless.org/dwm/dwm-$pkgver.tar.gz \
 	config.h
@@ -23,12 +24,16 @@
 build() {
   cd $srcdir/$pkgname-$pkgver
 
-  cp $srcdir/config.h config.h
+  cp $patchdir/config.h config.h
+  cp $patchdir/config.mk config.mk
 
   sed -i 's/CPPFLAGS =/CPPFLAGS +=/g' config.mk
   sed -i 's/CFLAGS =/CFLAGS +=/g' config.mk
   sed -i 's/LDFLAGS =/LDFLAGS +=/g' config.mk
 
+  patch -p1 < $patchdir/bstack.diff || return 1
+  patch -p1 < $patchdir/one_visible_noborder.diff || return 1
+
   make X11INC=/usr/include/X11 X11LIB=/usr/lib/X11 || return 1
   make PREFIX=/usr DESTDIR=$pkgdir install || return 1
 
