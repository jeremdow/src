--- PKGBUILD.orig	2011-05-19 19:15:30.000000000 -0400
+++ PKGBUILD	2011-05-19 19:17:48.000000000 -0400
@@ -22,6 +22,7 @@ optdepends=('tk: gitk and git gui'
 replaces=('git-core')
 provides=('git-core')
 backup=('etc/conf.d/git-daemon.conf')
+patchdir="$HOME/src/extra/git"
 source=("http://kernel.org/pub/software/scm/git/${pkgname}-${pkgver}.tar.bz2" \
         "http://kernel.org/pub/software/scm/git/git-manpages-${pkgver}.tar.bz2"
         git-daemon
@@ -31,6 +32,7 @@ changelog=ChangeLog
 build() {
   export PYTHON_PATH='/usr/bin/python2'
   cd "$srcdir/$pkgname-$pkgver"
+  patch -p1 < $patchdir/custom-header.diff || return 1
   make prefix=/usr gitexecdir=/usr/lib/git-core \
     CFLAGS="$CFLAGS" LDFLAGS="$LDFLAGS"
 
